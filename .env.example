# Example environment variables for MoneyMentorAI_Frontend
# Copy this file to `.env` (or `.env.local`) and fill in real keys.

# Gemini / LLM key (used by the Chatbot). Keep this secret — do NOT commit real keys.
VITE_GEMINI_KEY=

# News API key (used by News component). Keep this secret — do NOT commit real keys.
VITE_NEWS_API_KEY=

# Firebase config (if you use env-driven config, optionally add here)
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_APP_ID=
